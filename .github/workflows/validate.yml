name: Validation & Testing

on:
  pull_request:
    branches: [main]
    paths:
      - 'content/**'
      - 'scripts/**'
      - 'myst.yml'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/validate.yml'
  workflow_dispatch:

concurrency:
  group: validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Record validation start time
        run: |
          echo "BUILD_START_TIME=$(date +%s)" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV
          echo "# 📊 Validation Performance Metrics" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Validation started at: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Record checkout time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "CHECKOUT_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          # Note: All scripts are now JavaScript - 100% conversion complete!
          # See CONVERSION_STATUS.md for details

      - name: Record setup time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "SETUP_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Cache MyST build
        uses: actions/cache@v4
        with:
          path: ./_build
          key: myst-build-${{ github.sha }}
          restore-keys: |
            myst-build-

      - name: Install Node.js dependencies
        run: npm ci

      - name: Record npm install time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "NPM_INSTALL_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Run MyST Markdown Linter
        run: |
          echo "🔍 Running MyST Markdown Linter (JavaScript)..."
          npm run lint:quiet
        continue-on-error: true
        id: lint

      - name: Record lint time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "LINT_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Run Reference Validation
        run: |
          echo "🔗 Validating references and citations..."
          npm run validate-enhanced-quiet
        continue-on-error: true
        id: validate

      - name: Record validation time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "VALIDATE_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Run JavaScript Unit Tests
        run: |
          echo "🧪 Running JavaScript unit tests..."
          npm test
        continue-on-error: true
        id: tests

      - name: Record test time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "TEST_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Validate Documentation Links
        run: |
          echo "📄 Checking documentation links..."
          ERRORS=0

          # Check all markdown files in doc/ for broken internal links
          for file in doc/*.md doc/**/*.md; do
            [ -f "$file" ] || continue
            dir=$(dirname "$file")

            # Extract markdown links [text](path) and check if target exists
            while IFS= read -r link; do
              # Skip external links, anchors, and empty links
              [[ "$link" =~ ^https?:// ]] && continue
              [[ "$link" =~ ^# ]] && continue
              [[ -z "$link" ]] && continue

              # Remove anchor from link
              target="${link%%#*}"
              [[ -z "$target" ]] && continue

              # Resolve relative path
              if [[ "$target" = /* ]]; then
                full_path=".$target"
              else
                full_path="$dir/$target"
              fi

              # Check if file exists
              if [ ! -f "$full_path" ]; then
                echo "❌ Broken link in $file: $link -> $full_path not found"
                ERRORS=$((ERRORS + 1))
              fi
            done < <(grep -oP '\]\(\K[^)]+' "$file" 2>/dev/null || true)
          done

          if [ $ERRORS -gt 0 ]; then
            echo "Found $ERRORS broken documentation links"
            exit 1
          else
            echo "✅ All documentation links are valid"
          fi
        continue-on-error: true
        id: doclinks

      - name: Record doc link check time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "DOCLINK_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Build HTML Documentation (validation only)
        run: |
          echo "📚 Building HTML documentation..."
          npx myst build --html
        env:
          NODE_OPTIONS: "--max-old-space-size=8192"
        id: build

      - name: Record build time
        run: |
          STEP_END_TIME=$(date +%s)
          DURATION=$((STEP_END_TIME - STEP_START_TIME))
          echo "BUILD_TIME=$DURATION" >> $GITHUB_ENV
          echo "STEP_START_TIME=$(date +%s)" >> $GITHUB_ENV

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: html-build
          path: _build/html/
          retention-days: 5

      - name: Check Reports
        run: |
          echo "📋 Generated Reports:"
          if [ -d "reports" ]; then
            ls -lh reports/ || echo "No reports generated"
          else
            echo "No reports directory found"
          fi

      - name: Validation Summary
        if: always()
        run: |
          BUILD_END_TIME=$(date +%s)
          TOTAL_TIME=$((BUILD_END_TIME - BUILD_START_TIME))

          # Convert seconds to minutes:seconds format
          format_time() {
            local seconds=$1
            local minutes=$((seconds / 60))
            local remaining_seconds=$((seconds % 60))
            printf "%dm %02ds" $minutes $remaining_seconds
          }

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📋 Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status | Duration |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|----------|" >> $GITHUB_STEP_SUMMARY

          # Function to get status icon
          get_status() {
            case "$1" in
              success) echo "✅" ;;
              failure) echo "❌" ;;
              skipped) echo "⏭️" ;;
              *) echo "⚠️" ;;
            esac
          }

          echo "| MyST Linter | $(get_status '${{ steps.lint.outcome }}') ${{ steps.lint.outcome }} | $(format_time ${LINT_TIME:-0}) |" >> $GITHUB_STEP_SUMMARY
          echo "| Reference Validation | $(get_status '${{ steps.validate.outcome }}') ${{ steps.validate.outcome }} | $(format_time ${VALIDATE_TIME:-0}) |" >> $GITHUB_STEP_SUMMARY
          echo "| JavaScript Tests | $(get_status '${{ steps.tests.outcome }}') ${{ steps.tests.outcome }} | $(format_time ${TEST_TIME:-0}) |" >> $GITHUB_STEP_SUMMARY
          echo "| Doc Link Check | $(get_status '${{ steps.doclinks.outcome }}') ${{ steps.doclinks.outcome }} | $(format_time ${DOCLINK_TIME:-0}) |" >> $GITHUB_STEP_SUMMARY
          echo "| Build HTML | $(get_status '${{ steps.build.outcome }}') ${{ steps.build.outcome }} | $(format_time ${BUILD_TIME:-0}) |" >> $GITHUB_STEP_SUMMARY

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## ⏱️ Performance Breakdown" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Step | Duration | Percentage |" >> $GITHUB_STEP_SUMMARY
          echo "|------|----------|------------|" >> $GITHUB_STEP_SUMMARY

          # Function to add row with percentage
          add_row() {
            local name=$1
            local time=$2
            local formatted=$(format_time $time)
            local percentage=$((time * 100 / TOTAL_TIME))
            echo "| $name | $formatted | ${percentage}% |" >> $GITHUB_STEP_SUMMARY
          }

          add_row "Checkout repository" ${CHECKOUT_TIME:-0}
          add_row "Setup Node.js" ${SETUP_TIME:-0}
          add_row "npm install" ${NPM_INSTALL_TIME:-0}
          add_row "MyST Linter (JS)" ${LINT_TIME:-0}
          add_row "Reference Validation" ${VALIDATE_TIME:-0}
          add_row "JavaScript Tests" ${TEST_TIME:-0}
          add_row "Doc Link Check" ${DOCLINK_TIME:-0}
          add_row "Build HTML" ${BUILD_TIME:-0}

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🎯 Total Validation Time: $(format_time $TOTAL_TIME)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Validation completed at: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Add performance indicators
          echo "## 📈 Performance Indicators" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Check if validation is faster or slower than timeout
          TIMEOUT_SECONDS=$((15 * 60))
          USAGE_PERCENTAGE=$((TOTAL_TIME * 100 / TIMEOUT_SECONDS))

          if [ $USAGE_PERCENTAGE -lt 50 ]; then
            echo "✅ **Excellent**: Validation time is ${USAGE_PERCENTAGE}% of timeout limit" >> $GITHUB_STEP_SUMMARY
          elif [ $USAGE_PERCENTAGE -lt 75 ]; then
            echo "✓ **Good**: Validation time is ${USAGE_PERCENTAGE}% of timeout limit" >> $GITHUB_STEP_SUMMARY
          elif [ $USAGE_PERCENTAGE -lt 90 ]; then
            echo "⚠️ **Warning**: Validation time is ${USAGE_PERCENTAGE}% of timeout limit" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Critical**: Validation time is ${USAGE_PERCENTAGE}% of timeout limit" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Top 3 slowest steps:**" >> $GITHUB_STEP_SUMMARY

          # Create sorted list of times
          {
            echo "${BUILD_TIME:-0} Build HTML"
            echo "${TEST_TIME:-0} JavaScript Tests"
            echo "${VALIDATE_TIME:-0} Reference Validation"
            echo "${LINT_TIME:-0} MyST Linter (JS)"
            echo "${DOCLINK_TIME:-0} Doc Link Check"
            echo "${NPM_INSTALL_TIME:-0} npm install"
          } | sort -rn | head -3 | while read time name; do
            if [ $time -gt 0 ]; then
              echo "- $name: $(format_time $time)" >> $GITHUB_STEP_SUMMARY
            fi
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Console output
          echo "📊 Validation Summary"
          echo "===================="
          echo ""
          echo "✓ MyST Linter: ${{ steps.lint.outcome }}"
          echo "✓ Reference Validation: ${{ steps.validate.outcome }}"
          echo "✓ JavaScript Tests: ${{ steps.tests.outcome }}"
          echo "✓ Doc Link Check: ${{ steps.doclinks.outcome }}"
          echo "✓ Build Status: ${{ steps.build.outcome }}"
          echo ""
          echo "🎯 Critical Check Status:"
          if [ "${{ steps.build.outcome }}" = "success" ]; then
            echo "  ✅ HTML build succeeded - ready to merge!"
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### ✅ Ready to Merge" >> $GITHUB_STEP_SUMMARY
            echo "All critical checks passed!" >> $GITHUB_STEP_SUMMARY
          else
            echo "  ❌ Build failed - fix errors above"
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### ❌ Build Failed" >> $GITHUB_STEP_SUMMARY
            echo "Please fix the errors above before merging." >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
